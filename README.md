# Handwritten-Digit-Recognizer
A deep learning web app built with TensorFlow and Streamlit that predicts handwritten digits (0–9). Draw on the canvas or upload an image and get an instant prediction with confidence scores.
